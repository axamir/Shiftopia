name: Chain of Consent
on:
  issues:
    types: [closed]
jobs:
  chain:
    if: contains(github.event.issue.labels.*.name, 'vote')
    runs-on: ubuntu-latest
    steps:
      - name: Create block
        run: echo "Block would be added to CHAIN/ (mock)"
