name: Count Votes
on:
  issues:
    types: [closed]
jobs:
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  count:
    if: contains(github.event.issue.labels.*.name, 'vote')
    runs-on: ubuntu-latest
    steps:
      - name: Count approvals
        run: |
          echo "Vote counting simulation for issue #${{ github.event.issue.number }}"
          echo "::notice::Vote would be counted here. Result: Approved."
