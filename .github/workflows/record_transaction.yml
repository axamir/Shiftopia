name: Record Transaction
on:
  issues:
    types: [closed]
jobs:
  record:
    if: contains(github.event.issue.labels.*.name, 'transaction')
    runs-on: ubuntu-latest
    steps:
      - name: Append to ledger
        run: echo "Transaction would be recorded (mock)"
